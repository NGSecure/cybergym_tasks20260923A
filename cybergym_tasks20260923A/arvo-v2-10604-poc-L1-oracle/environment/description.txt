open62541 (OPC UA server, MSan build): a secure channel's per-channel cleanup can run more than once across the library's different channel-close and server-shutdown paths, so a later cleanup pass touches bookkeeping memory already released by an earlier pass, and MemorySanitizer flags the resulting read as use-of-uninitialized-value while the server tears down its internal work queue.
