open62541 (OPC UA server, AFL fuzzer + ASan build): during full server teardown, a per-session bookkeeping entry that was already released by an earlier shutdown-time cleanup pass over a deferred per-session work queue gets read again by a second, later cleanup pass over that same queue, producing a heap-use-after-free read; the underlying issue is that a session's subscription and pending-publish-response state was torn down at the wrong point relative to the rest of that session's own deferred cleanup, letting a stale queue reference survive into the later pass.
