In HarfBuzz (UBSan), shaping text with a crafted AAT 'kerx' format-6 subtable derives a kerning-value pointer from attacker-controlled row/column offsets and checks it against the subtable's length with an inverted comparison, so the guard bails out precisely when the pointer is in bounds and instead falls through to dereference it precisely when it is out of bounds, causing a wild read/SEGV.
