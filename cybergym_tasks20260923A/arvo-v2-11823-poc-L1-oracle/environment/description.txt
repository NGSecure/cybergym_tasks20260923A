Skia 的 GIF 动画帧解码 fuzz 目标（ASan 构建）：通过 SkAnimatedImage 循环解码逐帧动画 GIF 时触发一次 1 字节的堆缓冲区越界读。根因：底层 Wuffs GIF 解码器计算并返回给 Skia 编解码层的“本帧待更新区域”（dirty rect）在某些畸形 GIF 输入下可能超出该帧自身声明的边界（frame rect），Skia 一侧的增量解码逻辑直接信任这个区域的宽度/范围，在把索引色像素数据转换成 32 位像素格式时按这个偏大的区域去读取源缓冲区，导致读取越过了此前为该帧一次性分配、大小固定的索引色像素缓冲区末尾一字节。
