open62541 (OPC UA server, UBSan build, caught as a glibc allocator abort rather than a sanitizer diagnostic): the same double release of a secure channel's per-channel cleanup state -- once through its own close handling, again during the server's shutdown/deletion sequence -- corrupts glibc heap allocator metadata when freed a second time, aborting the process inside free() with a heap-consistency-check failure.
