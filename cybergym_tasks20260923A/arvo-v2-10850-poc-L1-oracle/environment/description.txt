在将解析后的 ODP action 重新格式化为字符串时发生 1 字节堆越界读。根因：属性长度校验逻辑发现某个 netlink 属性的实际长度与其类型期望长度不一致，于是转入通用逐字节打印分支，按该属性头部自称的长度（nl_attr_get_size()）遍历其 payload；但这个自称长度超出了该嵌套属性在解析阶段（构造 nlattr 缓冲区时）真正分配/写入的字节范围，导致读取越过堆缓冲区末尾。
