libgit2 (ASan heap-buffer-overflow read) occurs while parsing a commit object's signature line: the numeric string-to-integer conversion helper skips leading whitespace with a pointer walk that never checks the caller's remaining-length bound, so when the timestamp field runs all the way to the end of a length-bounded, non-NUL-terminated buffer, the scan steps one byte past the buffer's allocated end.
