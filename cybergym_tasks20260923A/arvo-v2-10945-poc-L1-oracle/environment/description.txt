解析 ODP action 字符串时，在 set(...) 分支里发生了一次未初始化栈内存读取。根因：该分支把内层字符串交给底层的 key/mask 属性解析函数处理；当内层字符串被识别为某种特殊前缀时，该函数会不做任何属性写入就提前返回“成功”，导致调用方栈上分配、从未清零或填充的 mask 缓冲区依旧是全未初始化状态；调用方随后无条件地读取这块 mask 缓冲区的属性长度字段，于是在解析阶段本身（尚未进入后续可能的格式化步骤）就直接消费了这段未初始化的栈内存。
